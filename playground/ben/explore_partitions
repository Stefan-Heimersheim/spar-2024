# %%
import pickle

# %%
# Unpickle the object
with open('/Users/benlerner/work/spar-2024/partitions/necessity_leiden_modularity_threshold_0.1_plotly.pkl', 'rb') as file:
    unpickled_object = pickle.load(file)

# Inspect the object
print(f"Type of the object: {type(unpickled_object)}")
print(f"Object content: {unpickled_object}")

# If it's a complex object, you might want to explore its attributes
if hasattr(unpickled_object, '__dict__'):
    print("Object attributes:")
    for attr, value in unpickled_object.__dict__.items():
        print(f"  {attr}: {value}")
# %%
unpickled_object

# %%
